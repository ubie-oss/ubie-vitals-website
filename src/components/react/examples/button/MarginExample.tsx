import { Button } from '@ubie/ubie-ui';
import type { FC } from 'react';

export const MarginExample: FC = () => {
  return (
    <Button ml="lg" mr="lg" mt="lg" mb="lg">
      Button (width margins)
    </Button>
  );
};
