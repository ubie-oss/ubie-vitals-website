---
title: 'RadioButton'
exampleKey: 'radio-button'
layout: '@layouts/ComponentLayout.astro'
description: 'リストの中から1つの項目を選択'
thumbnail: '/assets/images/components/thumbnail-radio-button.svg'
repositoryUrl: 'https://github.com/ubie-oss/ubie-ui/tree/main/src/components/RadioButton'
---

import SelectingFormComponents from '@sharedDocuments/form/SelectingFormComponents.md';

## Feature

<SelectingFormComponents />

## Usage

- 同じグループとなる項目には、同じ `name` 属性の値を設定します

### 項目をグルーピングし、タイトルをつける

できる限りグルーピングしてラベルをつけてください。選択肢を列挙しただけでは、何を意味する選択肢なのかをユーザーは理解できないかもしれません。

`<RadioGroup>`コンポーネントでまとめ、`label` propを指定してください。

```jsx
<RadioGroup label="今日の気分">
  <RadioButton name="condition">良い</RadioButton>
  <RadioButton name="condition">悪い</RadioButton>
</RadioGroup>

`<RadioButton>` を使用した選択肢が1つのコンテンツとして独立している場合`<section>` 要素で囲んでも良いでしょう。その場合、必ず見出し（`<hx>`） を添えてください。

```jsx
<section>
  <h2>本日の体調</h2>

  <RadioButton name="condition">良い</RadioButton>
  <RadioButton name="condition">悪い</RadioButton>
</section>
```

## Keyboard Interaction

- <kbd>Tab</kbd>キーでフォーカスすることができます
- カーソルキーの上下で選択を切り替えることができます
